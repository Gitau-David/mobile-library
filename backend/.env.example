# ─────────────────────────────────────────
#  Mobile Library Backend — Environment
# ─────────────────────────────────────────

PORT=5000

# MySQL connection
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_root_password
DB_NAME=mobile_library_db

# JWT — replace with a long random string in production
JWT_SECRET=change_this_to_a_long_random_secret_string
